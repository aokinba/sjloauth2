spring:
    application:
        name: client-a
    cloud:
        config:
            discovery:
              enabled: true
              service-id: SJL-CONFIG-GIT  # 注册中心的服务名
            profile: dev  # 指定配置文件的环境



